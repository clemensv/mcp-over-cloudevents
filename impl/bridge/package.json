{
  "name": "@modelcontextprotocol/server-bridge",
  "version": "0.1.0",
  "description": "MCP server bridge that wraps existing MCP servers and exposes them via WebSocket or MQTT",
  "license": "MIT",
  "type": "module",
  "bin": {
    "mcp-bridge": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && shx chmod +x dist/*.js",
    "prepare": "npm run build",
    "watch": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.26.0",
    "@types/node": "^22",
    "@types/ws": "^8.5.10",
    "cloudevents": "^8.0.0",
    "mqtt": "^5.3.6",
    "ws": "^8.16.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "shx": "^0.3.4",
    "typescript": "^5.6.2"
  }
}